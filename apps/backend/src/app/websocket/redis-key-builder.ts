import { Injectable } from '@nestjs/common'

// @Injectable()
// export class RedisKeyBuilder {
//   getAgentNodeTopicName(nodeName: string) {
//     return `razzle:agents:server-nodes:${nodeName}`
//   }

//   getAppIdRegistryKey(appId: string): string {
//     return `razzle:agent:node-apps:${appId}`
//   }

//   getClientNodeTopicName(nodeName: string): string {
//     return `razzle:clients:server-nodes:${nodeName}`
//   }

//   getClientIdRegistryKey(clientId: string): string {
//     return `razzle:client:node-client-ids:${clientId}`
//   }

//   getUserIdRegistryKey(userId: string): string {
//     return `razzle:client:node-user-ids:${userId}`
//   }
// }
